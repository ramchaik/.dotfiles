#-----------------
# Custom
#-----------------
alias c="clear"
alias grep='grep --colour=auto'
alias rr="source ~/.zshrc"
alias l="exa -alhgiHS"
alias ll="ls -lah"
alias ln="ln -v"
alias mkdir="mkdir -p"
alias ip="ipconfig getifaddr en0"
alias vim="nvim"
alias vi="nvim"

# Pretty print the path
alias path='echo $PATH | tr -s ":" "\n"'

#-----------------
# Tmux Swag
#-----------------
alias tx="./.dotfiles/scripts/tmux/sessionizer.sh"
alias ta="tmux a"
alias tks="tmux kill-server"

#-----------------
# Git
#-----------------
alias git="hub"
alias s="git status -sb"

# kubernetes
alias k="kubectl"

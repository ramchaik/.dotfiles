local options = {
  ensure_installed = { "lua", "javascript", "typescript", "json", "html", "css" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
